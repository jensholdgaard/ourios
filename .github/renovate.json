{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":semanticCommits",
    ":semanticCommitTypeAll(chore)",
    ":dependencyDashboard"
  ],
  "labels": [
    "dependencies"
  ],
  "prConcurrentLimit": 5,
  "schedule": [
    "before 06:00 on monday"
  ],
  "automergeStrategy": "squash",
  "packageRules": [
    {
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github-actions",
      "pinDigests": true,
      "matchUpdateTypes": [
        "digest",
        "pinDigest"
      ],
      "automerge": true
    },
    {
      "matchManagers": [
        "cargo"
      ],
      "groupName": "cargo (minor/patch)",
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": true
    },
    {
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "/^arrow(-[a-z]+)*$/"
      ],
      "allowedVersions": "<59",
      "description": "Hold the arrow crate family at 58.x. DataFusion 54 pins arrow ^58, so a standalone arrow 59 bump splits the workspace's arrow types and breaks the build. Lift together with the DataFusion 55 upgrade (RFC 0021 phase 2, which also gates the thrift advisory, #295) - the same pattern as the object_store hold below."
    },
    {
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "object_store"
      ],
      "allowedVersions": "<0.14.0",
      "description": "Hold object_store at 0.13.x. DataFusion 53 pins object_store ^0.13, and ourios-querier hands ourios-parquet's Store::object_store() to DataFusion's register_object_store (RFC 0013 \u00a72.2), so the two crates must share one object_store version. Bumping to 0.14 splits the Arc<dyn ObjectStore> type at that boundary and breaks the build. Lift this together with a DataFusion upgrade (which also gates the thrift advisory, #295)."
    },
    {
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "reqwest"
      ],
      "allowedVersions": "<0.13.0",
      "description": "Hold reqwest at 0.12.x. reqwest 0.13 drops the rustls-tls-manual-roots feature ourios-ingester's OTLP exporter path uses, and object_store 0.12/0.13 still require reqwest ^0.12, so a standalone bump fails resolution (Renovate PR #505's plan failure). Lift together with the DataFusion 55 upgrade (RFC 0021 phase 2, #295) - the same pattern as the arrow and object_store holds above."
    },
    {
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "p256"
      ],
      "allowedVersions": "<0.14.0",
      "description": "Hold p256 at 0.13.x. jsonwebtoken pins the RustCrypto 0.13-era stack (elliptic-curve/ecdsa/pkcs8), and p256 0.14 moves to the split rand-core/pkcs8 APIs (OsRng relocation, to_pkcs8_pem removal) that broke Renovate PR #505's clippy. Lift when jsonwebtoken ships a RustCrypto-0.14-era release; run cargo tree -i p256 first."
    },
    {
      "matchManagers": [
        "helm-values"
      ],
      "pinDigests": false,
      "description": "Don't digest-pin the chart's default image. image.tag is deliberately empty (floating latest; the values.yaml comment tells operators to pin a released tag in production), so a point-in-time digest in the published chart's defaults contradicts that contract - and renovate writes it as a bare @sha256:... tag, which the ourios.image helper renders as an invalid repo:@sha256:... reference."
    }
  ]
}
